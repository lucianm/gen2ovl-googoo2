diff -Naur xmltv2vdr_orig/dist/epgdata2xmltv/Makefile xmltv2vdr/dist/epgdata2xmltv/Makefile
--- xmltv2vdr_orig/dist/epgdata2xmltv/Makefile	2012-11-05 17:53:49.000000000 +0100
+++ xmltv2vdr/dist/epgdata2xmltv/Makefile	2012-11-05 17:54:58.763330267 +0100
@@ -7,7 +7,7 @@
 CXX      ?= g++
 CXXFLAGS ?= -g -rdynamic -O2 -Wall -Wextra -Woverloaded-virtual -Wno-parentheses
 PKG-CONFIG ?= pkg-config
-STRIP ?= strip
+STRIP ?= -s
 
 ### Includes and Defines (add further entries here):
 
@@ -29,7 +29,7 @@
 
 ### install directory ###
 
-INSTALL = /usr/bin
+INSTALL = $(DESTDIR)/usr/bin
 
 ### The object files (add further files here):
 
@@ -64,12 +64,12 @@
 	$(CXX) $(CXXFLAGS) $(OBJS) $(LIBS) -o $@
 
 install: epgdata2xmltv
-	@cp epgdata2xmltv $(INSTALL)
-	$(STRIP) $(INSTALL)/epgdata2xmltv
-	@mkdir -p /var/lib/epgsources
-	@mkdir -p /var/lib/epgsources/epgdata2xmltv-img
-	@cp epgdata2xmltv.dist /var/lib/epgsources/epgdata2xmltv
-	@chown -R vdr.vdr /var/lib/epgsources
+	@mkdir -p $(INSTALL)
+	@install $(STRIP) epgdata2xmltv $(INSTALL)
+	@mkdir -p $(DESTDIR)/var/lib/epgsources
+	@mkdir -p $(DESTDIR)/var/lib/epgsources/epgdata2xmltv-img
+	@cp epgdata2xmltv.dist $(DESTDIR)/var/lib/epgsources/epgdata2xmltv
+	@chown -R vdr.vdr $(DESTDIR)/var/lib/epgsources
 
 dist: clean
 	@-rm -rf $(TMPDIR)/epgdata2xmltv
